node_modules
dist
.DS_Store
*.local
.env
.env.*.local
.vite
# Regenerated by build-and-push.sh from .docker-version each run; the bare
# version string itself isn't worth tracking, only the counter is.
VERSION
